vcs-facade:
  vcs:
    services:
      - id: test-bitbucket
        type: BITBUCKET
        http-url: http://${test.vcs-host}/
        ssh-url: ssh://git@${test.vcs-host}/
        username: admin
        password: admin
  opensearch:
    enabled: false
