vcs-facade:
  vcs:
    bitbucket:
      instances:
        - host: http://${test.vcs-host}/
          username: admin
          password: admin
    gitea:
      enabled: false
  opensearch:
    enabled: false
