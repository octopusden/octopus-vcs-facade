vcs-facade:
  vcs:
    bitbucket:
      enabled: false
    gitea:
      host: http://${test.vcs-host}/
      username: test-admin
      password: test-admin
      index:
        scan:
          delay: 5000
  opensearch:
    host: ${test.opensearch-host}
    ssl: false
    username: admin
    password: admin
    index:
      suffix: ut
