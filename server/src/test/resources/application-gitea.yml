vcs-facade:
  vcs:
    services:
      - type: GITEA
        host: http://${test.vcs-host}/
        username: test-admin
        password: test-admin
  opensearch:
    host: ${test.opensearch-host}
    ssl: false
    username: admin
    password: admin
    index:
      suffix: ut
      scan:
        delay: 5000
