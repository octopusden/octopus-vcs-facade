{{- if eq .Values.profile "gitea" }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "opensearch.fullname" . }}-route
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "opensearch.fullname" . }}-route
spec:
  port:
    targetPort: 9200
  to:
    kind: Service
    name: {{ include "opensearch.fullname" . }}-service
{{- end }}